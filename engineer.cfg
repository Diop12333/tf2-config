// INITIALIZING CONFIG
exec dimitri_binds.cfg

// CUSTOM CONFIG
bind "z" "voicemenu 1 6"
bind "v" "voicemenu 0 1"

// Build Sentry
bind "c" "destroy 2; build 2"
bind "q" "destroy 0; build 0"
